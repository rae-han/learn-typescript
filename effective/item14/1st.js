"use strict";
const distance = (a, b) => (Math.sqrt(Math.pow(a.x - b.x, 2) + Math.sqrt(Math.pow(a.y - b.y, 2))));
const me = {
    firstName: 'raehan',
    lastName: 'jeong',
    birth: new Date()
};

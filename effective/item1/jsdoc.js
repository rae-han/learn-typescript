/**
 * A function that returns a character by combining the two values when a name and gender are entered.
 * @param {string} name input name
 * @param {string} gender input gender
 * @returns {string}
 */
function hello(name, gender) {
  return name + gender;
}

hello('raehan', 'male')